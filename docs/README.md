Open the generated dashboard after a scan:

```bash
python -m us_stock_alerts --config config.example.yaml --no-notify --all --serve
```

Then visit http://127.0.0.1:8765/dashboard.html

A snapshot from 2026-09-18 is in `out/dashboard.html` after you run the scan.
